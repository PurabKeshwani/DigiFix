

buildscript {
    dependencies {
        // Add this line
        classpath 'com.google.gms:google-services:4.4.1' // Check for the latest version if needed
    }
}

plugins {
alias(libs.plugins.android.application) apply false
}